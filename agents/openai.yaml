interface:
  display_name: "/xxd-panel-045"
  short_description: "Create source-aware XXD Panel 045 artwork"
  default_prompt: "Use $xxd-panel-045 with my photograph, ask for modes and copy settings, then create fresh raster outputs."
policy:
  allow_implicit_invocation: true
